# build artifacts
*.tar.gz
/*.Rcheck/

# run outputs and artifacts, regenerated by the scripts
/cache/
/figures/

# runs/ holds versioned per-site run configs; run outputs under each are ignored
/runs/*
# the combined site_info.csv and any docs live directly under runs/
!/runs/*.csv
!/runs/README.md
!/runs/*/
/runs/*/*
!/runs/*/*.yml
!/runs/*/*.yaml
!/runs/*/*.csv
!/runs/*/*.xml
!/runs/*/*.json
!/runs/*/README.md

# r session files
.Rhistory
.RData
.Rproj.user/
