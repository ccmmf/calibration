% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/method_eki.R
\name{run_eki}
\alias{run_eki}
\title{Run ensemble Kalman inversion with likelihood tempering}
\usage{
run_eki(y, U, fwd, Sig, n_itr = 1L, par_map = NULL, G0 = NULL)
}
\arguments{
\item{y}{numeric vector length P: the observation.}

\item{U}{matrix (J, D): initial parameter ensemble (constrained space).}

\item{fwd}{function(U, itr) -> matrix (J, P): the forward model, vectorized
over ensemble members (rows).}

\item{Sig}{matrix (P, P): Gaussian likelihood covariance.}

\item{n_itr}{integer: number of tempering iterations.}

\item{par_map}{list(fwd, inv) of transport maps from get_par_map_funcs(); if
NULL the update runs in the native space.}

\item{G0}{matrix (J, P): optional precomputed fwd(U) for the first iteration.}
}
\value{
list with U (final ensemble, constrained space), par_map, eki_list
  (per iteration run_eki_step outputs), and G (last forward outputs).
}
\description{
Tempers the one-step update over n_itr iterations. Each iteration
evaluates the forward model, applies the EnKF update in unconstrained space
(if par_map is given), and maps back. The final ensemble approximates the
posterior p(u | y): exact for a linear-Gaussian problem, approximate
otherwise. The update combines columns positionally, so the forward output,
the observation, and the likelihood covariance are aligned by slot name up
front; a differently ordered forward output would otherwise condition each
observation on the wrong model output and yield a plausible but wrong
posterior with no error.
}
\author{
Meng Lai and Akash BV
}
