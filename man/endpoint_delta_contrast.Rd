% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/observation_operator.R
\name{endpoint_delta_contrast}
\alias{endpoint_delta_contrast}
\title{Contract two-date observations into an endpoint change and change contrasts}
\usage{
endpoint_delta_contrast(
  obs,
  reps,
  variable,
  control,
  t0_year,
  t1_year,
  new_variable = paste0(variable, "_delta")
)
}
\arguments{
\item{obs}{a build_obs target list(y, Sigma, meta).}

\item{reps}{replicate rows (treatment_id, replicate_id, obs_year, value) for
the variable, one row per plot per date.}

\item{variable}{the per treatment variable to contract.}

\item{control}{treatment id the changes are compared against.}

\item{t0_year, t1_year}{the two observation years.}

\item{new_variable}{name for the resulting variable.}
}
\value{
an obs list carrying the change slots, with the contraction recorded
  in `transform`.
}
\description{
One slot for the control's change between two dates and one per
treatment for its change relative to the control's (the double difference).
Values come from the cached cells. The uncertainty comes from replicate rows
paired by replicate id across the two dates, so each change carries the
paired variance rather than the sum of two cross sectional ones. Contrasts
share the control's change, and that covariance is kept.
}
\author{
Akash BV
}
